# Allena

The deployed build of Allena, a personal training app. Source lives in a
private repository; this repo holds only the shipped files.

- `/` — the app
- `/sandbox/` — demo account with sample data
